import { AnimatedCollapseDirective } from './animated-collapse.directive';

describe('DirPfAnimatedCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
